# coding: utf-8

import numpy as np
import matplotlib.pyplot as plt
from scipy import stats
r = uniform.rvs(size=1000)
from scipy.stats import uniform
r = uniform.rvs(size=1000)
r.shape
r
r.info90
r.info()
r = uniform.rvs(size=(1000,1000))
r.shape()
r
r = np.array(r)
r.shape()
r.shape
y_vec = np.sum(r,axis=0)
plt.hist(y_vec)
y_vec.shape
plt.show()
